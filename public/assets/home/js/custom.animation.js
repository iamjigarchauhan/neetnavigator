$(document).ready(function(){
    
  $(window).scroll(function() {
    var scrollPositions = $(this).scrollTop();



    // banner
    if (scrollPositions >= 0 && scrollPositions <= 1400) {
      $('.elementA').addClass('active fadeInUp animated');
    } else {
      $('.elementA').removeClass('active fadeInUp animated');
    }


    if (scrollPositions >= 0 && scrollPositions <= 1400) {
      $('.elementB').addClass('active fadeIn animated');
    } else {
      $('.elementB').removeClass('active fadeIn animated');
    }

    // banner

    
    if (scrollPositions >= 0 && scrollPositions <= 150) {
      $('.element1').addClass('active');
    } else {
      $('.element1').removeClass('active');
    }



  
    if (scrollPositions >= 100 && scrollPositions <= 1400) {
      $('.element2').addClass('active fadeInUp animated');
    } else {
      $('.element2').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 110 && scrollPositions <= 1600) {
      $('.element33').addClass('active fadeInUp animated');
    } else {
      $('.element33').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 700 && scrollPositions <= 1900) {
      $('.element3').addClass('active fadeInUp animated');
    } else {
      $('.element3').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 1200 && scrollPositions <= 3000) {
      $('.element30').addClass('active fadeInRight animated');
    } else {
      $('.element30').removeClass('active fadeInRight animated');
    }


    if (scrollPositions >= 800 && scrollPositions <= 2500) {
      $('.element8').addClass('active fadeInUp animated');
    } else {
      $('.element8').removeClass('active fadeInUp animated');
    }


    if (scrollPositions >= 1700 && scrollPositions <= 3000) {
      $('.element4').addClass('active fadeInUp animated');
    } else {
      $('.element4').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 1000 && scrollPositions <= 3900) {
      $('.element4b').addClass('active slideInRight animated');
    } else {
      $('.element4b').removeClass('active slideInRight animated');
    }


    
    if (scrollPositions >= 1200 && scrollPositions <= 2900) {
      $('.element4bf').addClass('active fadeInRight animated');
    } else {
      $('.element4bf').removeClass('active fadeInRight animated');
    }


    if (scrollPositions >= 2500 && scrollPositions <= 3900) {
      $('.element5').addClass('active fadeInUp animated');
    } else {
      $('.element5').removeClass('active fadeInUp animated');
    }


   
    if (scrollPositions >= 3000 && scrollPositions <= 4000) {
      $('.element55').addClass('active fadeInUp animated');
    } else {
      $('.element55').removeClass('active fadeInUp animated');
    }


    if (scrollPositions >= 1900 && scrollPositions <= 3500) {
      $('.element52').addClass('active fadeInUp animated');
    } else {
      $('.element52').removeClass('active fadeInUp animated');
    }

    // overview pages

    if (scrollPositions >= 1000 && scrollPositions <= 4000) {
      $('.element310').addClass('active fadeInUp animated');
    } else {
      $('.element310').removeClass('active fadeInUp animated');
    }

    // ATMIA's Smart Admission Counseling Solutions section

    if (scrollPositions >= 100 && scrollPositions <= 4000) {
      $('.element305A').addClass('active fadeInUp animated');
    } else {
      $('.element305A').removeClass('active fadeInUp animated');
    }
    
    if (scrollPositions >= 100 && scrollPositions <= 4000) {
      $('.element305B').addClass('active fadeInUp animated');
    } else {
      $('.element305B').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 300 && scrollPositions <= 4000) {
      $('.element305C').addClass('active fadeInUp animated');
    } else {
      $('.element305C').removeClass('active fadeInUp animated');
    }
    
    if (scrollPositions >= 300 && scrollPositions <= 4000) {
      $('.element305D').addClass('active fadeInUp animated');
    } else {
      $('.element305D').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 500 && scrollPositions <= 4000) {
      $('.element305E').addClass('active fadeInUp animated');
    } else {
      $('.element305E').removeClass('active fadeInUp animated');
    }
    
    if (scrollPositions >= 500 && scrollPositions <= 4000) {
      $('.element305F').addClass('active fadeInUp animated');
    } else {
      $('.element305F').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 700 && scrollPositions <= 4000) {
      $('.element305G').addClass('active fadeInUp animated');
    } else {
      $('.element305G').removeClass('active fadeInUp animated');
    }
    
    if (scrollPositions >= 700 && scrollPositions <= 4000) {
      $('.element305H').addClass('active fadeInUp animated');
    } else {
      $('.element305H').removeClass('active fadeInUp animated');
    }
    
    if (scrollPositions >= 100 && scrollPositions <= 4000) {
      $('.element304').addClass('active fadeInUp animated');
    } else {
      $('.element304').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 1500 && scrollPositions <= 5000) {
      $('.element305').addClass('active fadeInUp animated');
    } else {
      $('.element305').removeClass('active fadeInUp animated');
    }

    // overview 2
    if (scrollPositions >= 200 && scrollPositions <= 4000) {
      $('.element211').addClass('active fadeInRight animated');
    } else {
      $('.element211').removeClass('active fadeInRight animated');
    }

    if (scrollPositions >= 700 && scrollPositions <= 4000) {
      $('.element212').addClass('active fadeInUp animated');
    } else {
      $('.element212').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 800 && scrollPositions <= 4000) {
      $('.element215').addClass('active fadeInUp animated');
    } else {
      $('.element215').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 1850 && scrollPositions <= 4000) {
      $('.element216').addClass('active fadeInUp animated');
    } else {
      $('.element216').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 100 && scrollPositions <= 4000) {
      $('.element217').addClass('active fadeIn animated');
    } else {
      $('.element217').removeClass('active fadeIn animated');
    }

    // contact card

    if (scrollPositions >= 600 && scrollPositions <= 4000) {
      $('.element501').addClass('active fadeInUp animated');
    } else {
      $('.element501').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 110 && scrollPositions <= 4000) {
      $('.element502').addClass('active fadeInUp animated');
    } else {
      $('.element502').removeClass('active fadeInUp animated');
    }
    if (scrollPositions >= 120 && scrollPositions <= 4000) {
      $('.element503').addClass('active fadeInUp animated');
    } else {
      $('.element503').removeClass('active fadeInUp animated');
    }

    if (scrollPositions >= 300 && scrollPositions <= 4000) {
      $('.element504').addClass('active fadeInUp animated');
    } else {
      $('.element504').removeClass('active fadeInUp animated');
    }
    if (scrollPositions >= 400 && scrollPositions <= 4000) {
      $('.element505').addClass('active fadeInUp animated');
    } else {
      $('.element505').removeClass('active fadeInUp animated');
    }

  });

});
